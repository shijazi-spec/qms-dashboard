COPY ONLY THE "scripts" SECTION BELOW INTO REPLIT package.json
Replace your current "scripts" block with this (same content + one new line):

  "scripts": {
    "dev": "mastra dev",
    "build": "mastra build",
    "check": "tsc",
    "check:format": "prettier --check '**/*.ts'",
    "format": "prettier --write '**/*.ts'",
    "qc": "npx tsx tests/qc/run-platform-qc.ts"
  },

Keep "dependencies" and "devDependencies" exactly as they are. Do NOT add "format" or "qc" inside devDependencies.

# ExampleOrg — Status Snapshot

**Date:** 2026-05-12
**Repo path:** `QMS Platform/ExampleOrg/`
**Branch:** `slack-audit-notification-alignment` (up to date with `origin`)
**Last release:** v4.<REDACTED_PHONE>— Internal Audits re-architecture, Manual Audit Intake (`/intake`), External Audits (`/external-audits`), Admin & Tools nav dropdown, migration templates.

---

## Working tree

Dirty. ~15 modified files, ~8 untracked files (~347 insertions / ~30 deletions overall).

### Modified

| Area | Files |
|------|-------|
| Mastra core | `src/mastra/index.ts`, `src/mastra/routes/callIntelligenceRoutes.ts`, `src/mastra/tools/callIngestTool.ts` |
| Utils | `src/utils/callIntelligenceDb.ts`, `src/utils/rbacMiddleware.ts` |
| Dashboard UI | `dashboard/calls.html` (+212 lines), `dashboard/audit-readiness.html`, `dashboard/compliance.html`, `dashboard/qms-docs.html` |
| i18n | `dashboard/i18n/en.json`, `dashboard/i18n/ar.json` |
| CI | `.github/workflows/ai-approval-redaction-integration.yml`, `.github/workflows/streaming-download-smoke.yml` |
| Scripts / QC | `scripts/build-i18n.cjs`, `tests/qc/platform-qc-manifest.ts` |

### Untracked (new work in progress)

Theme: **Call Intelligence / SDR governance** feature.

- `src/mastra/routes/mcpCallEvaluationRoutes.ts`
- `src/utils/callLeadPhoneMatch.ts`
- `src/utils/callMcpImportSources.ts`
- `src/utils/callMcpReconciliation.ts`
- `src/utils/sdrGovernanceRulesEngine.ts`
- `src/utils/sdrProcessScope.ts`
- `tests/vitest/sdrGovernanceRulesEngine.vitest.test.ts`
- `src/config/` (new directory)

---

## Recent commits (last 10)

```
29e8b5f Ship full workspace updates including digest delivery fixes
b81115d Align direct-audit finding-type separators with digest format
43d69a0 Ensure audit completion always triggers Slack notification path
ae28590 Polish Slack digest visuals with clearer section separators
7ec96cc Improve Slack digest readability and metric clarity
ee02926 Unify audit notification delivery through outbox pipeline
d1dcaf0 Stabilize digest delivery text and add run-history observability
004bf8e Fix executive digest syntax corruption on main path
4aeb2d4 Harden digest smoke workflow against missing CI prerequisites
24e6d93 Stop noisy CI email failures from heavy integration workflows
```

Branch theme: **Slack audit-notification delivery via the outbox pipeline.**

---

## Environment

- `node_modules/` **not installed** — run `npm install` before `npm run dev | check | qc`.
- Stack: Mastra + Inngest + libSQL / Postgres, OpenAI / OpenRouter providers, Slack Web API, Resend, Playwright + Vitest.
- Scripts: `dev` (`mastra dev`), `build` (`mastra build`), `check` (`tsc`), `qc` (`tsx tests/qc/run-platform-qc.ts`).

---

## Summary

Two parallel streams of work on the same branch:

1. **Slack audit-notification alignment** (committed) — outbox-pipeline unification, digest readability, smoke-test hardening.
2. **Call Intelligence / SDR governance** (uncommitted) — new MCP call-evaluation route, lead-phone matching, MCP reconciliation, SDR rules engine + process scope, new `calls.html` UI surface, i18n strings (EN + AR), QC manifest entries.

Suggested next steps: install deps, run `npm run check` and `npm run qc` against the WIP, then decide whether the SDR work belongs on this branch or should be split off before merge.

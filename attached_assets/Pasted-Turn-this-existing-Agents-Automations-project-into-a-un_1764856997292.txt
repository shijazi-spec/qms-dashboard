Turn this existing “Agents & Automations” project into a unified app that also serves a simple web UI.
I want one project that keeps all current agents and workflows, and adds a lightweight web server to show my dashboards.

What I need you to build

Add a lightweight web server

Use Node.js with a minimal web framework (for example Express, or anything equivalent you recommend).

The server should run inside this same project without breaking or changing any existing agent / automation functionality.

When I start the app, both the agents/automations and the web server should be available.

Serve my existing HTML pages as real screens

I already have these files in the project:

dashboard/index.html → main quality dashboard

admin.html → Quality Admin Panel (governance, scorecards)

qms.html → Quality Management System page

Configure the web server so that:

Opening the root URL (/) loads dashboard/index.html.

Opening /admin loads admin.html.

Opening /qms loads qms.html.

Make sure all related assets (CSS, JS, images) used by these pages are served correctly.

Keep the project fully compatible with Replit

Configure the Run / Start command so that when I run the project, Replit gives me a public URL where I can:

See the dashboard at /

See the admin panel at /admin

See the QMS page at /qms

I understand the internal “Preview” panel for Agents may still say “Preview not available”; that’s fine.
I want a browser link that opens the web UI.

Do NOT break existing agents & workflows

All existing workflows in the Playground must continue to work as they are.

The new web server is an addition, not a replacement.

Simple navigation (optional but preferred)

If possible, add a simple top navigation bar on each page with links:

“Dashboard” → /

“Admin” → /admin

“QMS” → /qms

Use my existing styling (Tailwind) or minimal styles so everything looks consistent.

Acceptance criteria

When I run the project, Replit shows me a URL (for example https://<project>.replit.app).

If I open that URL in my browser:

/ displays my dashboard interface (not raw HTML code).

/admin displays the Quality Admin Panel UI.

/qms displays the QMS page.

My existing automations and agents still run via Playground as before.

No manual copying of files to a new project is required; everything stays inside this current project.
Files updated in this session — exactly 4
All four sit in QMS Platform\QMS-Dashboard\_replit_deploy\ with their Replit paths preserved. Just upload this folder to Replit, overwriting the matching paths.

#	Path (relative to project root)	What changed
1
src/utils/duplicateRadarDatabase.ts
Rewrote generateSmartRecommendation() to be module-aware (MERGE vs LINK vs CLOSE vs REVIEW), Account-first primary selection. Added removeRecordsByZohoIds() helper. Added safety comment on markStaleRecords().
2
src/utils/zohoCRM.ts
Added fetchDeletedZohoRecords() helper that calls Zoho's /crm/v2/{module}/deleted?type=all endpoint — this is what detects records you merged or deleted directly inside Zoho.
3
src/mastra/routes/duplicateRadarRoutes.ts
Imported the two new helpers. Replaced the buggy markStaleRecords(seenZohoIds) call in incremental mode with a proper per-module deletion-detection pass. markStaleRecords now runs only in full-sync mode. /api/duplicates/ai-recommendations/:clusterId now returns primary_type and is_cross_module.
4
dashboard/duplicates.html
Cluster modal: new MERGE/LINK/CLOSE/REVIEW color-coded badges, "Cross-module cluster" tag, explainer banner (MERGE = same-module Zoho merge, LINK = set Account_Name/Contact_Name), record-type tag next to each recommendation, clearer Resolve button label, post-merge sync hint.
Nothing else was touched
I did not edit any other file under QMS-Dashboard/. In particular:

No changes to package.json, tsconfig.json, .env.example, .gitignore, README.md, src/mastra/index.ts, src/mastra/inngest/index.ts, dashboard/crm.html, or dashboard/js/navigation.js — those still hold the edits from earlier sessions.
The old DEPLOYMENT_*.md and REPLIT_DEPLOYMENT_INSTRUCTIONS.md files in the project root were not modified; they're stale docs from previous deployments. I deliberately left them alone so I don't accidentally touch your deployment history — delete them yourself if you want.
The WalaPlus_Platform_SOP.md (one level up, in QMS Platform/) also does not need re-uploading for this fix.
If your editor still shows the old content
That's a stale view inside the editor, not the file on disk. I confirmed with the real filesystem timestamps (all four files written today between 1:14–1:18 AM). To force your editor to reload:

Close the file tab.
Re-open it from the file tree, or press Ctrl+Shift+P → "Revert File".
What to tell Replit
Please replace these 4 files. No schema changes, no new dependencies, no new routes:

src/utils/duplicateRadarDatabase.ts
src/utils/zohoCRM.ts
src/mastra/routes/duplicateRadarRoutes.ts
dashboard/duplicates.html
Purpose:

Duplicate Radar recommendations now follow Zoho's data model: same-module → MERGE, cross-module (Contact/Deal under an Account) → LINK via Account_Name/Contact_Name, superseded Leads → CLOSE. Primary selection prefers Accounts.
Fixed an incremental-sync bug where markStaleRecords was wrongly flagging unmodified records as stale (it was being called with a partial ID list). It now runs only in full-sync mode.
Added deletion detection via Zoho's GET /crm/v2/{module}/deleted?type=all, so records you merge or delete directly inside Zoho CRM are automatically purged from the duplicate radar at the next sync (every 6 hours, or immediately via the "Scan from Zoho" button).
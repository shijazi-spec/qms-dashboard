I've reviewed the full SOP (v3.8) against our deployed code. There are 6 discrepancies between the SOP and what was actually implemented. Here's what needs to be corrected -- either in the SOP or on Replit:

1. Model: GPT-4o vs GPT-4o-mini (Critical)
SOP says (Section 4.21, 6.2): GPT-4o Replit deployed: GPT-4o-mini (per their confirmation message)

Fix on Replit: Change the model in src/mastra/agents/qmsConsultantAgent.ts to gpt-4o as designed, OR update SOP Sections 4.21 and 6.2 to say GPT-4o-mini if cost is a concern.

2. Alert API Endpoints Mismatch (Section 4.21)
SOP says:

PATCH /api/consultant/alerts/:id/read
DELETE /api/consultant/alerts/:id
Our actual code has:

POST /api/consultant/alerts/:id/acknowledge
POST /api/consultant/alerts/:id/resolve
POST /api/consultant/alerts/:id/dismiss
Fix: The SOP's endpoint table needs to be corrected to match the actual implementation. Tell Replit:

In the SOP Section 4.21, replace the alerts endpoint table with: | /api/consultant/alerts/:id/acknowledge | POST | Acknowledge an alert | | /api/consultant/alerts/:id/resolve | POST | Mark alert as resolved | | /api/consultant/alerts/:id/dismiss | POST | Dismiss an alert |

3. Duplicate Section Numbering (Section 8.3)
SOP has two "Section 8.3":

8.3 Automated Triggers (renamed from 8.2)
8.3 External Notification Triggers
Fix: The second 8.3 should be 8.4.

4. Background Scanner Thresholds (Section 8.2)
SOP says: "High-severity risks (score >= 12)" Our code: (likelihood * impact) >= 15

SOP says: "Audit score decline (latest score below 85%)" Our code: Checks for declining trend across 3+ audits with >5% drop (not a fixed 85% threshold)

SOP says: "PDPL compliance gaps (data incidents without resolution)" Our code: Checks 3 things -- empty data inventory, no active AI guardrails, AND open data incidents

Fix: Update SOP Section 8.2 to match actual thresholds, OR adjust the scanner code in src/utils/aiBackgroundScanner.ts to match the SOP.

5. AI SDK Version Reference (Section 6.2)
SOP says: "Uses @ai-sdk/openai-v5" Our code: Uses @ai-sdk/openai (the createOpenAI import)

Fix: Minor -- verify the actual package name in package.json and correct the SOP reference.

6. Sandbox Module Removal (Section 4.20)
SOP v3.8 says sandbox was removed, but this was done by Replit, not by us.

Verify on Replit: Confirm that /sandbox route, navigation link, and sandbox.html were actually removed. If not, either remove them or restore the SOP reference.

Summary of What to Send to Replit
Please fix these 6 items to keep the SOP and code in sync:

Change agent model from gpt-4o-mini to gpt-4o in qmsConsultantAgent.ts (or update SOP if keeping mini)
Fix SOP Section 4.21 alert endpoints: they should be POST acknowledge, resolve, dismiss (not PATCH read and DELETE)
Fix SOP Section 8.3 numbering: second 8.3 should be 8.4
Fix SOP Section 8.2 scanner thresholds to match actual code: risk score >= 15, audit decline = trend-based, PDPL checks 3 conditions
Fix SOP Section 6.2: @ai-sdk/openai not @ai-sdk/openai-v5
Confirm sandbox removal was completed